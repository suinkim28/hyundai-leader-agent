---
description: 결재·보고자료 검토 — 근거 부족과 상충 지점 표시
argument-hint: [문서 경로 또는 제목]
---

`ROUTINES.md` 의 **R5 — 결재·보고자료 검토** 를 실행한다. 대상: $ARGUMENTS

문서를 주장 단위로 분해하고, 각 주장을 셋 중 하나로 판정한다.

| 판정 | 뜻 |
| --- | --- |
| 근거 확인 | 문서 안이나 원본 시스템에서 확인됨 |
| **근거 부족** | 수치·출처가 없거나, 출처가 문서 자신뿐 |
| **상충** | 다른 자료·과거 결정과 어긋남 |

- 수치는 원본과 대조하고 합계를 직접 계산한다.
- `decisions/` 에서 과거 같은 사안의 결정을 찾아 비교한다. 방침이 바뀌었으면
  그 사실을 표시한다.
- **결재 여부는 말하지 않는다.** 판단은 본부장의 것이다.
- 전자결재 승인·반려는 하지 않는다. 사내 결재시스템은 연동 대상이 아니다.

`briefings/$(date +%F)_<문서명>_결재검토.md` 로 저장한다.
