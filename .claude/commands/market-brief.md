---
description: 시장 브리핑 (06:00), 관심 주제 동향을 3건 이내로
---

`ROUTINES.md` 의 **R1: 시장 동향 브리핑** 을 실행한다.

먼저 `PROFILE.md` 의 관심 주제와 `ORG.md` 의 본부 최우선 목표를 읽는다.
관심 주제가 `[미확보]` 면 그 사실을 알리고, 본부 과제 기준으로만 수집한다.

```bash
bin/graph market --output logs/<오늘>/market_raw.md
```

수집한 것에서 중복을 제거하고, 본부 과제와 연결되는 것만 남긴다.
**3건을 넘기지 않는다.** 고를 수 없으면 "특기사항 없음"이 맞는 답이다.

`briefings/<오늘>_시장브리핑.md` 로 저장하고, 요약만 화면에 낸다.
무인 실행 중일 수 있으므로 확인이 필요한 행동은 하지 않는다.
