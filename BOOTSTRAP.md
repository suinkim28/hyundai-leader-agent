# BOOTSTRAP.md: 개인화 절차

이 스켈레톤은 백지 상태로 배포된다. 여기서부터 본부장 개인의 비서가 된다.

**설계 전제 하나:** 개인화는 한 번에 끝나지 않는다. 세션 2시간 안에 64개
질문을 던지면 본부장은 지치고, 답변의 절반은 부정확하다. 그래서 **꼭 필요한
꼭 필요한 것만 먼저 확보하고, 나머지는 매일 일하면서 쌓는다.**

| 단계 | 누가 | 언제 | 무엇 |
| --- | --- | --- | --- |
| 0단계 | AX 챔피언 / DEP | 1회차 D-3까지 | 연결 진단, 조직 정보 초안 작성 |
| 1단계 | 본부장 | 1회차 세션 20분 | 사전자료 확인 → 부족한 것만 질문 → 즉시 Quick Win 1건 시연 |
| 2단계 | 에이전트 | 자율 활용 기간 매일 | 일하면서 알게 된 것을 자동 축적 |
| 3단계 | 본부장 + 챔피언 | 2회차 세션 | 축적 결과 확인, 규칙 교정, 범위 확장 |

세션에서 새로 설치하거나 연결하지 않는다. 세션 시간은 **본부장의 지식과
선호를 넣는 데만** 쓴다.

---


## 0단계: 챔피언 사전 준비 (1회차 D-3까지)

본부장이 백지에서 시작하지 않도록 챔피언이 초안까지 만들어 둔다.

### 0-0. 개인 파일 만들기 (처음 한 번)

`PROFILE.md` 와 `ORG.md` 가 없으면 `templates/` 의 빈 원본을 같은 이름으로
복사한다. 에이전트가 파일 도구로 하거나, PowerShell 로 한다.

```powershell
Copy-Item templates\PROFILE.md PROFILE.md
Copy-Item templates\ORG.md ORG.md
```

두 파일은 본부장 개인 정보다. **다른 본부장에게 넘길 때는 이 두 파일과
산출물 폴더를 비운 스켈레톤을 쓴다** (`DEVELOPMENT.md` 배포 전 확인).
본부장은 세션에서 **고치기만** 한다.

### 0-1. 연결 진단

```powershell
bin\graph.cmd check
```

결과를 그대로 `logs/YYYY-MM-DD/connections.txt` 에 남긴다. 막힌 항목이 있으면
D-3 시점에 에스컬레이션한다. 세션 당일에 발견하면 이미 늦다.

### 0-2. `ORG.md` 초안

본부 조직도, 보고 라인, 진행 중인 주요 과제, 자주 등장하는 인물을 채운다.
이것은 본부장에게 묻지 않아도 챔피언이 알 수 있는 정보다. **본부장의 시간을
조직도 받아쓰기에 쓰지 않는다.**

### 0-3. 데이터 샘플 확보

1회차에서 Quick Win을 **실제 데이터로** 시연하려면 다음이 미리 있어야 한다.

- [ ] 최근 2주 메일 (미읽음 포함): 실제 상태 그대로
- [ ] 회사 캘린더 2주치
- [ ] 최근 회의 자료 1건 (첨부 PPT/문서 포함)
- [ ] 진행 중 과제 1건의 흩어진 이력 (메일, Confluence, 구두 지시)
- [ ] **사전설문 응답, 사전인터뷰 전사본** → `attachments/raw/`. 1단계가 이것을
      먼저 읽는다. 없으면 본부장께 처음부터 묻게 된다

연결이 막혔으면 본부장 동의를 받아 내보내기 파일로 대체한다
(`CONNECTIONS.md` 대체 경로).


---

## 1단계: 이미 있는 것부터 읽는다 (세션 5분)

**본부장님은 이미 사전설문 14문항에 답하셨고, 60분 사전인터뷰도 하셨다.**
같은 것을 다시 묻는 것은 그 시간을 없던 일로 만드는 것이다.
**질문 목록을 처음부터 훑지 않는다.**

### 순서

#### 1. 워크스페이스에 자료가 있는지 먼저 본다

챔피언이 미리 넣어 두었을 수 있다. 묻기 전에 확인한다.

`attachments/raw/` 와 `knowledge_base/` 를 파일 도구(Glob)로 훑는다. 셸을 쓰지 않는다.

전사본, 설문 결과, 프로파일 카드가 있으면 읽고 **3번으로 건너뛴다.**

#### 2. 없으면 딱 하나만 여쭙는다

> 본부장님께 맞춰 드리려면 사전에 하셨던 **인터뷰와 설문 내용**이 필요합니다.
> 그 내용을 이 창에 붙여넣어 주시거나, 파일을 주시면 제가 읽겠습니다.

- **붙여넣으시면** 그대로 받아 `knowledge_base/사전자료_<날짜>.md` 로 저장한다
- **파일을 주시겠다고 하면** `attachments/raw/` 에 넣어 달라고 안내하고 다시 읽는다
- **"없다" 또는 "그냥 물어봐라"** 하시면 그때만 아래 폴백 질문으로 간다

이 질문 하나로 시작한다. 다른 질문을 함께 얹지 않는다.

#### 3. 읽고 PROFILE.md 를 채운 뒤, 채운 것을 보여드린다

사전설문 14문항은 `PROFILE.md` 의 다음 항목에 그대로 대응한다.

| 설문 문항 | PROFILE.md 항목 |
| --- | --- |
| 생성형 AI 사용 빈도 / 주로 쓰는 AX 도구 / 사용 방식 | 3. 정보 환경 |
| 생성형 AI pain point | 5. 페인포인트 |
| 가장 많은 시간을 쓰는 경영 업무 | 2. 업무 실태 |
| 그 업무에서 가장 불편한 점 | 5. 페인포인트, Quick Win 후보 |
| 업무 정보를 어디서 확인하는가 | 3. 정보 환경 |
| 선호하는 보고, 소통 방식 | 4. 답변, 소통 선호 |
| Agent 가 먼저 지원할 결과물 | 5. Quick Win 후보 |
| Agent 답변 선호 방식 | 4. 답변 규칙 |
| 2주 후 성공 기준 | 5. 성공 기준 |
| 자유 서술 | 전 항목의 근거 |

인터뷰 전사본에서는 설문이 못 담는 것을 읽는다. 실제 하루의 흐름, 이해관계자,
설문 보기로는 잡히지 않는 맥락이다.

**전사본이 설문과 어긋나면 전사본을 따른다.** 설문 보기가 여러 항목을 한 묶음으로
제시해(`이메일 및 Teams`) 실제와 다르게 읽히는 경우가 있다. 어긋난 지점은
`PROFILE.md` 의 `설문, 인터뷰 불일치` 에 이유까지 적는다.

채운 뒤 이렇게 보여드린다. **목록을 나열하지 말고 5줄 이내로 요약한다.**

> 사전자료에서 이만큼 파악했습니다.
> 시간을 가장 많이 쓰시는 일은 ...이고, 정보는 주로 ...에 있으며,
> 답변은 ...를 선호하신다고 되어 있습니다.
> 확인 없이 하면 안 되는 것은 ...입니다.
> 이 중 지금과 달라진 것이 있으면 말씀해 주십시오.

#### 4. 자료로도 안 채워진 것만 묻는다

설문과 인터뷰에 없는 항목은 보통 이 셋뿐이다. **이것만 묻는다.**

1. **올해 본부의 최우선 목표**는 무엇입니까?
   > 설문에 없다. 브리핑에서 무엇을 위로 올릴지가 여기서 갈린다.

2. 직접 하시는 일과 **보고받아 판단**하시는 일의 비율은 대략 어떻습니까?
   > 판단 중심이시면 프롬프트를 직접 치시게 하지 않고, 챔피언이 실행하고
   > 본부장이 결과를 보고 고치는 구조로 간다.

3. **브리핑을 언제, 어디서** 받고 싶으십니까? (시각 / 화면, 메일, 메시지)
   > 설문은 "선호하는 보고 방식"까지만 받았고 시각과 경로는 없다.

메일 수신량은 묻지 않는다. Graph 가 붙어 있으면 그 자리에서 세어 본다.

### 그리고 바로 Quick Win

1. `PROFILE.md` 의 `상태:` 를 `진행중` 으로 바꾼다.
2. **설문의 "가장 불편한 점" 또는 "먼저 지원할 결과물" 을 그 자리에서 실행해 보여준다.**
   설명하지 말고 돌린다. 실제 본부장 데이터로, 실제 결과를 화면에 띄운다.
3. 결과를 보고 본부장이 "이건 이렇게 해달라"고 하시는 것: 그것이 진짜 개인화다.
   그 지시를 `PROFILE.md` 에 즉시 적는다.
4. 이 왕복을 2~3회 반복하면 1회차 세션의 목적은 달성된 것이다.

**시간 배분은 확인 5분, 시연이 나머지 전부다.** 질문에 시간을 쓰지 않는다.

---

## 폴백: 사전자료가 하나도 없을 때만

인터뷰도 설문도 없는 본부장(예: 뒤늦게 합류하신 분)에게만 쓴다.
**자료가 있으면 이 목록을 꺼내지 않는다.**

**A. 나는 누구인가**
1. 직책과 담당 범위를 한 문장으로. 본부의 올해 최우선 목표는 무엇입니까?
2. 하루 중 가장 시간을 많이 쓰는 일 세 가지는 무엇입니까?
3. 직접 하시는 일과 보고받아 판단하시는 일의 비율은 어떻습니까?

**B. 정보가 어디 있는가**
4. 업무 정보가 주로 어디에 있습니까? (메일 / Teams / Confluence / SharePoint /
   OneNote / 종이, 구두)
5. 보고는 주로 어떤 형태로 받으십니까? (PPT 첨부 / 대면 / 메일 본문 / 문서 링크)

**C. 무엇이 불편한가**
6. 없어졌으면 하는 반복 작업 한 가지만 꼽는다면 무엇입니까?
   > Quick Win 후보다. 하나만 받는다. 여러 개를 받으면 하나도 못 끝낸다.
7. 지금까지 AI 를 써 보셨다면 기대에 못 미쳤던 지점은 무엇이었습니까?

**D. 어떻게 답해야 하는가**
8. 답변은 어떤 형태를 선호하십니까?
   (결론 먼저 / 근거 함께 / 대안 2~3개 비교 / 표 / 원문 인용)
9. 브리핑을 언제, 어디서 받고 싶으십니까?

**진행 원칙**

- 한 번에 하나씩 묻는다. 목록을 통째로 보여주지 않는다.
- 답을 들을 때마다 `PROFILE.md` 를 즉시 갱신하고 무엇이 기록됐는지 한 줄로 보여준다.
- "잘 모르겠다"는 훌륭한 답이다. `[미확보]` 로 적고 넘어간다. 캐묻지 않는다.
- 본부장이 직접 타이핑하게 하지 않는다. 말씀하시면 에이전트가 적는다.

---

## 2단계: 자율 활용 중 누적 학습

**이 단계가 이 스켈레톤의 핵심이다.** 챔피언들이 우려한 "일회성으로 끝나는 것
아닌가"에 대한 답이 여기 있다. 세션에서 만든 것이 아니라, 매일 조금씩 자라는
구조가 남아야 한다.

### 무엇을 언제 기록하는가

| 신호 | 기록 위치 | 조건 |
| --- | --- | --- |
| "이건 이렇게 해줘" 형태의 교정 | `PROFILE.md` → 답변 규칙 | 즉시 |
| 같은 지시가 3회 반복 | `PROFILE.md` → 답변 규칙 | 3회째에 |
| 처음 보는 인물, 조직, 약어 | `ORG.md` | 즉시 |
| 회의에서 내려진 결정과 근거 | `decisions/` | 그날 안에 |
| 과제 상태, 리스크 변화 | `projects/YYYY-MM-DD-<이름>/` | 변화 시 |
| 본부장이 루틴 밖으로 지시한 작업 | `projects/YYYY-MM-DD-<이름>/README.md` | 지시받은 즉시 (`SYSTEM.md` §8-2-1) |
| 녹음 가능/불가 회의 구분 | `PROFILE.md` → 녹음 정책 | 확인될 때 |

### 하루 한 번 강제되는 지점

17:00 회고 루틴이 이 갱신을 매일 강제한다 (`ROUTINES.md` R8).
회고에서 반드시 확인한다.

- 오늘 본부장이 내 답을 고친 적이 있는가 → 무엇을 어떻게 고쳤는가
- 오늘 처음 나온 인물, 용어가 있는가
- 오늘 내려진 결정 중 기록되지 않은 것이 있는가

**세 개 다 "없음"이면 그날은 기억이 자라지 않은 것이다.** 그 상태가 사흘
연속이면 본부장에게 알린다. 에이전트가 쓰이지 않고 있다는 뜻이거나, 학습
포착이 고장 난 것이다.

### 메일, Teams 에서 추론해 채우는 것

메일 발신·수신 패턴, Teams 대화, 회의 참석 이력 같은 실제 데이터에서
근거 있게 추론한 선호, 업무 방식도 `PROFILE.md` 에 적는다. 이때는 반드시
출처에 `(추론: 메일/Teams 등 근거)` 를 남겨 본인 진술과 구분한다. 본인이
맞다고 확인하면 `(추론)` 을 지우고 확정값으로 바꾸고, 아니라고 하면 즉시
지운다. **추론임을 표시하지 않고 확정 진술처럼 적지 않는다.**

### 기록하지 않는 것

- 오늘만 유효한 것 (오늘 회의 시각, 일회성 요청)
- 원본 시스템에 이미 있는 것 (메일 본문, Jira 이슈 내용)
- 근거 데이터 없이 순수 상상으로 채운 것
- 비밀번호, 토큰, 개인정보 값

---

## 3단계: 2회차 세션

1. `PROFILE.md` / `ORG.md` 를 본부장과 함께 읽는다. **2주 만에 얼마나 자랐는지
   보여주는 것 자체가 이 과정의 성과다.**
2. 틀리게 학습한 것을 지운다. 잘못된 기억은 없는 기억보다 나쁘다.
3. 자율 활용 중 막혔던 지점을 진단한다: 연결 문제인가, 규칙 문제인가,
   기대 불일치인가.
4. 새 루틴을 추가한다. 다만 **한 번에 하나만** 늘린다.
5. `PROFILE.md` 와 `ORG.md` 의 `상태:` 를 `완료` 로 바꾼다. 이후에도 2단계 누적은 계속된다.

---

## 부트스트랩 대화 예시

> **본부장:** 오늘부터 비서 역할 좀 해줘.
>
> **에이전트:** 본부장님께 맞춰 드리려면 사전에 하셨던 인터뷰와 설문 내용이
> 필요합니다. 그 내용을 이 창에 붙여넣어 주시거나 파일을 주시면 제가 읽겠습니다.
>
> **본부장:** (자료 제공)
>
> **에이전트:** 사전자료에서 이만큼 파악했습니다. (5줄 요약) 이 중 지금과
> 달라진 것이 있으면 말씀해 주십시오.
>
> **본부장:** 달라진 건 없어요.
>
> **에이전트:** 자료로 확인되지 않은 것 하나만 여쭙겠습니다. 올해 본부의
> 최우선 목표는 무엇입니까?
>
> **본부장:** (답변)
>
> **에이전트:** 기록했습니다.
> `PROFILE.md → 직책, 담당범위 / 본부 최우선 목표`
> 다음: 하루 중 가장 시간을 많이 쓰시는 일 세 가지는 무엇입니까?

일반적인 조언으로 답하지 않는다. **묻는 쪽이 된다.**
